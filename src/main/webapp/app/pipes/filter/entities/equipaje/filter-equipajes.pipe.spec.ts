import { FilterEquipajesPipe } from './filter-equipajes.pipe';

describe('FilterEquipajesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEquipajesPipe();
    expect(pipe).toBeTruthy();
  });
});
