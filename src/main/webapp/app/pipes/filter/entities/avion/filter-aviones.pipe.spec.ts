import { FilterAvionesPipe } from './filter-aviones.pipe';

describe('FilterAvionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAvionesPipe();
    expect(pipe).toBeTruthy();
  });
});
