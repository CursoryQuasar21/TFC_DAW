import { FilterCiudadesPipe } from './filter-ciudades.pipe';

describe('FilterCiudadesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCiudadesPipe();
    expect(pipe).toBeTruthy();
  });
});
