import { FilterVuelosPipe } from './filter-vuelos.pipe';

describe('FilterVuelosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVuelosPipe();
    expect(pipe).toBeTruthy();
  });
});
