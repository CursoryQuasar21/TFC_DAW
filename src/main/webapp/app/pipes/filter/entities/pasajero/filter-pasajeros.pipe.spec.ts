import { FilterPasajerosPipe } from './filter-pasajeros.pipe';

describe('FilterPasajerosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPasajerosPipe();
    expect(pipe).toBeTruthy();
  });
});
