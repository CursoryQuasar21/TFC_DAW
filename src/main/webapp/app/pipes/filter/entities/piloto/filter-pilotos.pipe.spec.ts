import { FilterPilotosPipe } from './filter-pilotos.pipe';

describe('FilterPilotosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPilotosPipe();
    expect(pipe).toBeTruthy();
  });
});
