import { FilterTripulantesPipe } from './filter-tripulantes.pipe';

describe('FilterTripulantesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTripulantesPipe();
    expect(pipe).toBeTruthy();
  });
});
