import { FilterModelosPipe } from './filter-modelos.pipe';

describe('FilterModelosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModelosPipe();
    expect(pipe).toBeTruthy();
  });
});
