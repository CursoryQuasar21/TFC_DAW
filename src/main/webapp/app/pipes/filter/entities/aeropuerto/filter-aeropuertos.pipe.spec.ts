import { FilterAeropuertosPipe } from './filter-aeropuertos.pipe';

describe('FilterAeropuertosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAeropuertosPipe();
    expect(pipe).toBeTruthy();
  });
});
